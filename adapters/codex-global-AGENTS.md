# Global Engineering Defaults

- Detect the project stack before editing.
- Follow project-local AGENTS.md first when more specific.
- Preserve runtime compatibility.
- Keep changes scoped.
- Apply secure coding, tests, documentation and native language conventions.
- For Sankhya Java projects, use the repository's `stacks/java-sankhya/` rules and detect legacy vs modern SDK before generating platform-specific APIs.
