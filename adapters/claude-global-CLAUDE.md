# Global Engineering Defaults

Apply project-local standards before global preferences.
Detect stack and runtime before changing code.
Keep modifications scoped and compatible.
For Sankhya Java projects, consult the Java Sankhya standard and determine legacy vs modern SDK first.
